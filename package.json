{
  "name": "piax-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "nest build",
    "render-build": "npm run build && npx prisma migrate deploy && npx prisma generate",
    "start": "node dist/main.js",
    "start:dev": "nest start --watch",
    "migrate:dev": "prisma migrate dev && prisma generate"
  },
  "dependencies": {
    "@nestjs/axios": "^4.0.1",
    "@nestjs/common": "^11.0.1",
    "@nestjs/config": "^4.0.2",
    "@nestjs/core": "^11.0.1",
    "@nestjs/platform-express": "^11.0.1",
    "@prisma/client": "^6.13.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@nestjs/testing": "^11.1.28",
    "@types/node": "^26.1.1",
    "prisma": "^6.13.0",
    "typescript": "^5.7.3"
  }
}
